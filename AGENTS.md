## Spec

This is an app for creating animations using HTML and CSS. It works by letting users define **keyframes** and then automatically generating the transitions between those keyframes.

- A "keyframe" is a combination of HTML markup (which includes Tailwind classes) and custom CSS styles
